<?xml version="1.0" encoding="utf-8"?>
<!--
  ============================================================
  Windows 11 Unattended Answer File — TEMPLATE
  ============================================================
  DO NOT use this file directly on a USB drive.
  Run Build-USB.ps1 to generate autounattend.xml from config.ps1.

  Placeholders (substituted by Build-USB.ps1):
    %%ORGANIZATION%%       Company/org name
    %%TIMEZONE%%           Windows timezone string
    %%WINDOWS_EDITION%%    "Windows 11 Pro" / "Windows 11 Home" / etc.
    %%ITADMIN_USERNAME%%   Local IT admin account username
    %%ITADMIN_DISPLAY%%    IT admin display name (full name)
    %%ITADMIN_PASSWORD%%   IT admin account password (plain text on USB only)

  USB STRUCTURE (after running Build-USB.ps1):
    USB root/
    ├── autounattend.xml        (generated — not this template)
    ├── [Windows 11 ISO files]
    └── Setup/
        ├── Setup.ps1
        ├── packages.json
        ├── config.example.ps1
        └── config.ps1          (your secrets — gitignored)

  DELL OPTIPLEX 3070/5070 MFF NOTES:
    - No driver injection needed. Windows 11 inbox drivers cover
      Intel I219 NIC, UHD 630 graphics, Realtek audio.
    - Dell Command | Update (in packages.json) handles the rest
      post-install (BIOS updates, firmware, chipset).
    - TPM 2.0 is present on these models; BitLocker will work.
  ============================================================
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend"
          xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

  <!-- ========================================================
       PASS 1: windowsPE
       Runs in Windows PE before files are copied.
       Sets installer locale and configures disk layout.
       ======================================================== -->
  <settings pass="windowsPE">

    <component name="Microsoft-Windows-International-Core-WinPE"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">
      <SetupUILanguage>
        <UILanguage>en-US</UILanguage>
      </SetupUILanguage>
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UILanguageFallback>en-US</UILanguageFallback>
      <UserLocale>en-US</UserLocale>
    </component>

    <component name="Microsoft-Windows-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">

      <UserData>
        <AcceptEula>true</AcceptEula>
        <FullName>%%ITADMIN_DISPLAY%%</FullName>
        <Organization>%%ORGANIZATION%%</Organization>
        <!--
          Product key: remove this block to use the OEM digital license
          embedded in the device firmware (standard on Dell retail units).
          Only add a key here for volume licensing / KMS scenarios.
        -->
        <!-- <ProductKey>
          <Key>XXXXX-XXXXX-XXXXX-XXXXX-XXXXX</Key>
          <WillShowUI>Never</WillShowUI>
        </ProductKey> -->
      </UserData>

      <!-- ====================================================
           DISK LAYOUT — UEFI/GPT (erases Disk 0 entirely)
           Partition map:
             1. EFI System Partition  (100 MB, FAT32)
             2. Microsoft Reserved    ( 16 MB, no filesystem)
             3. Windows OS            (all remaining space, NTFS)
           Windows Setup creates the WinRE recovery partition
           automatically at the end — no need to define it here.
           ==================================================== -->
      <DiskConfiguration>
        <WillShowUI>OnError</WillShowUI>
        <Disk wcm:action="add">
          <DiskID>0</DiskID>
          <WillWipeDisk>true</WillWipeDisk>

          <CreatePartitions>
            <CreatePartition wcm:action="add">
              <Order>1</Order>
              <Type>EFI</Type>
              <Size>100</Size>
            </CreatePartition>

            <CreatePartition wcm:action="add">
              <Order>2</Order>
              <Type>MSR</Type>
              <Size>16</Size>
            </CreatePartition>

            <!-- Extend=true takes all remaining disk space -->
            <CreatePartition wcm:action="add">
              <Order>3</Order>
              <Type>Primary</Type>
              <Extend>true</Extend>
            </CreatePartition>
          </CreatePartitions>

          <ModifyPartitions>
            <ModifyPartition wcm:action="add">
              <Order>1</Order>
              <PartitionID>1</PartitionID>
              <Label>System</Label>
              <Format>FAT32</Format>
            </ModifyPartition>

            <!-- MSR: no format, no label -->
            <ModifyPartition wcm:action="add">
              <Order>2</Order>
              <PartitionID>2</PartitionID>
            </ModifyPartition>

            <ModifyPartition wcm:action="add">
              <Order>3</Order>
              <PartitionID>3</PartitionID>
              <Label>Windows</Label>
              <Letter>C</Letter>
              <Format>NTFS</Format>
            </ModifyPartition>
          </ModifyPartitions>
        </Disk>
      </DiskConfiguration>

      <!-- Select the Windows edition to install.
           Common values: "Windows 11 Pro", "Windows 11 Home",
                          "Windows 11 Enterprise" -->
      <ImageInstall>
        <OSImage>
          <WillShowUI>OnError</WillShowUI>
          <InstallTo>
            <DiskID>0</DiskID>
            <PartitionID>3</PartitionID>
          </InstallTo>
          <InstallFrom>
            <MetaData wcm:action="add">
              <Key>/IMAGE/NAME</Key>
              <Value>%%WINDOWS_EDITION%%</Value>
            </MetaData>
          </InstallFrom>
        </OSImage>
      </ImageInstall>

    </component>
  </settings>

  <!-- ========================================================
       PASS 2: specialize
       Runs after first boot, before OOBE, as SYSTEM.
       Handles: computer name, time zone, and — critically —
       auto-copying the Setup\ folder from the USB to C:\Setup
       so it is available when the user logs in.
       ======================================================== -->
  <settings pass="specialize">

    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">
      <!--
        Temporary name used only during installation.
        Setup.ps1 renames the machine to its real name on first run.
      -->
      <ComputerName>WIN11-SETUP</ComputerName>
      <TimeZone>%%TIMEZONE%%</TimeZone>
      <RegisteredOrganization>%%ORGANIZATION%%</RegisteredOrganization>
    </component>

    <component name="Microsoft-Windows-Deployment"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">
      <RunSynchronous>

        <!--
          Scan all drive letters for the Setup\ folder (identified by
          the presence of Setup\Setup.ps1). Copies it to C:\Setup so
          it persists after the USB is removed.

          IMPORTANT: Both commands are wrapped in try/catch and always
          exit 0. A failure here must never abort Windows Setup — the
          IT admin can copy the Setup folder manually if needed.
        -->
        <RunSynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Description>Copy provisioning files from USB to C:\Setup</Description>
          <Path>powershell.exe -NonInteractive -Command "try { foreach ($drive in [System.IO.DriveInfo]::GetDrives()) { $marker = [IO.Path]::Combine($drive.RootDirectory.FullName, 'Setup', 'Setup.ps1'); if (Test-Path $marker) { Copy-Item -Path ([IO.Path]::Combine($drive.RootDirectory.FullName, 'Setup')) -Destination 'C:\' -Recurse -Force; break } } } catch {} ; exit 0"</Path>
        </RunSynchronousCommand>

        <!-- Set PowerShell execution policy so Setup.ps1 can run -->
        <RunSynchronousCommand wcm:action="add">
          <Order>2</Order>
          <Description>Allow PowerShell script execution</Description>
          <Path>powershell.exe -NonInteractive -Command "try { Set-ExecutionPolicy RemoteSigned -Scope LocalMachine -Force } catch {} ; exit 0"</Path>
        </RunSynchronousCommand>

      </RunSynchronous>
    </component>

  </settings>

  <!-- ========================================================
       PASS 3: oobeSystem
       Runs during OOBE.
       Handles: skip all OOBE screens, create local IT admin
                account, auto-logon once, launch Setup.ps1.
       ======================================================== -->
  <settings pass="oobeSystem">

    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">

      <!-- ====================================================
           OOBE BYPASS
           HideOnlineAccountScreens forces the local account path
           on Windows 11 Pro, bypassing Microsoft account entirely.
           ==================================================== -->
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideLocalAccountScreen>false</HideLocalAccountScreen>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>false</HideWirelessSetupInOOBE>
        <!-- 1=Express settings, 3=Skip privacy settings screen -->
        <ProtectYourPC>3</ProtectYourPC>
        <SkipMachineOOBE>true</SkipMachineOOBE>
        <SkipUserOOBE>true</SkipUserOOBE>
        <UnattendEnableRetailDemo>false</UnattendEnableRetailDemo>
      </OOBE>

      <!-- ====================================================
           IT ADMIN LOCAL ACCOUNT
           Password comes from %%ITADMIN_PASSWORD%% — substituted
           by Build-USB.ps1 at USB prep time, never stored in git.
           ==================================================== -->
      <UserAccounts>
        <LocalAccounts>
          <LocalAccount wcm:action="add">
            <Name>%%ITADMIN_USERNAME%%</Name>
            <DisplayName>%%ITADMIN_DISPLAY%%</DisplayName>
            <Group>Administrators</Group>
            <Password>
              <Value>%%ITADMIN_PASSWORD%%</Value>
              <PlainText>true</PlainText>
            </Password>
          </LocalAccount>
        </LocalAccounts>
      </UserAccounts>

      <!-- ====================================================
           AUTO LOGON (one time only)
           Logs in as the IT admin so Setup.ps1 runs on first boot.
           LogonCount=1 — subsequent logins require password.
           ==================================================== -->
      <AutoLogon>
        <Enabled>true</Enabled>
        <Username>%%ITADMIN_USERNAME%%</Username>
        <LogonCount>1</LogonCount>
        <Password>
          <Value>%%ITADMIN_PASSWORD%%</Value>
          <PlainText>true</PlainText>
        </Password>
      </AutoLogon>

      <!-- ====================================================
           LAUNCH Setup.ps1 ON FIRST LOGIN
           Runs from C:\Setup (copied there during specialize).
           -NoExit keeps the window open for interactive prompts.
           ==================================================== -->
      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Description>Launch provisioning script</Description>
          <CommandLine>powershell.exe -NoExit -ExecutionPolicy Bypass -File "C:\Setup\Setup.ps1"</CommandLine>
          <RequiresUserInput>true</RequiresUserInput>
        </SynchronousCommand>
      </FirstLogonCommands>

    </component>

    <!-- OS locale (separate from installer locale above) -->
    <component name="Microsoft-Windows-International-Core"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UILanguageFallback>en-US</UILanguageFallback>
      <UserLocale>en-US</UserLocale>
    </component>

  </settings>

</unattend>
